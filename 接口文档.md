# 网上书店后端接口文档

> 根据 `D:\no game\Code\DatabaseHomework\demo` 当前源码整理。  
> 默认服务地址为 `http://localhost:8080`，如果修改了端口，请替换文档中的 `BASE_URL`。
>
> 当前文档对应后端已启用 Service 层兜底校验、订单支付过期校验和
> `spring.jpa.hibernate.ddl-auto=validate`。

## 1. 通用约定

### 1.1 请求头

除登录、注册以及公开查询接口外，都需要携带：

```http
Authorization: Bearer <JWT_TOKEN>
Content-Type: application/json
```

公开的 GET 接口：

- `/api/books/**`
- `/api/categories`
- `/api/authors`
- `/api/publishers`

`/api/admin/**` 除了需要 JWT，还必须是 `ADMIN` 角色。

### 1.2 统一响应

成功响应：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {}
}
```

失败响应：

```json
{
  "code": 400,
  "message": "请求参数不正确",
  "data": null
}
```

说明：

- 业务成功时响应体中的 `code` 固定为 `200`。
- 创建接口的 HTTP 状态码为 `201`，但响应体 `code` 仍为 `200`。
- 常见错误码：`400` 参数错误、`401` 未登录或 Token 失效、`403` 无权限、`404` 资源不存在、`409` 状态冲突或数据重复、`500` 服务器错误。
- 前端应同时判断 HTTP 状态码和响应体 `code`。

### 1.3 分页结构

分页参数：

| 参数 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `page` | number | `1` | 从 1 开始 |
| `size` | number | `10` | 最大值为 100 |

分页响应：

```json
{
  "records": [],
  "total": 0,
  "page": 1,
  "size": 10,
  "totalPages": 0
}
```

### 1.4 枚举

| 枚举 | 可选值 |
| --- | --- |
| 图书状态 `BookStatus` | `ON_SALE`、`OFF_SALE` |
| 订单状态 `OrderStatus` | `PENDING_PAYMENT`、`PENDING_SHIPMENT`、`SHIPPED`、`COMPLETED`、`CANCELLED` |
| 支付方式 `PaymentMethod` | `ALIPAY`、`WECHAT_PAY`、`BANK_CARD`、`MOCK` |
| 支付状态 `PaymentStatus` | `PENDING`、`SUCCESS`、`FAILED`、`CLOSED`、`REFUNDED` |
| 用户角色 `Role` | `ADMIN`、`CUSTOMER` |
| 分类状态 | `0` 停用、`1` 启用 |

### 1.5 数据库与编码前置条件

- 启动应用前请按顺序执行 `sql/01_schema.sql`、`sql/02_data.sql`。
- 应用使用 `ddl-auto=validate`，不会自动创建或修改数据库表结构；如果实体和数据库结构不一致，应用会启动失败。
- Java、配置文件和 SQL 文件统一使用 UTF-8；MySQL 数据库和连接使用 `utf8mb4`。
- 前端收到的时间为 ISO-8601 格式，例如 `2026-08-05T10:20:00`。

## 2. 认证接口

### 2.1 登录

```http
POST /api/auth/login
```

请求体：

```json
{
  "username": "reader",
  "password": "password"
}
```

字段限制：用户名 3-30 个字符，密码 6-50 个字符。

响应 `data`：

```json
{
  "id": 2,
  "username": "reader",
  "nickname": "Book Reader",
  "role": "CUSTOMER",
  "token": "eyJ..."
}
```

前端登录成功后保存 `token`，之后请求统一添加 `Authorization: Bearer <token>`。

### 2.2 注册

```http
POST /api/auth/register
```

请求体：

```json
{
  "username": "new_reader",
  "password": "password",
  "nickname": "新用户",
  "email": "reader@example.com",
  "phone": "13800000000"
}
```

说明：

- 用户名只能包含字母、数字和下划线。
- 注册成功后直接返回登录信息和 JWT。
- 新注册用户角色固定为 `CUSTOMER`，状态固定为启用。
- 用户名重复返回 `409`。

## 3. 公共图书与基础数据接口

### 3.1 图书列表

```http
GET /api/books
```

查询参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `keyword` | string | 否 | 按书名模糊查询 |
| `categoryId` | number | 否 | 按分类查询 |
| `authorId` | number | 否 | 按作者查询 |
| `publisherId` | number | 否 | 按出版社查询 |
| `page` | number | 否 | 默认 `1` |
| `size` | number | 否 | 默认 `10`，最大 `100` |

注意：多个筛选条件同时传入时只使用一个，优先级为：

```text
keyword > categoryId > authorId > publisherId
```

只返回 `ON_SALE` 图书。

列表记录字段：

```json
{
  "id": 1,
  "isbn": "9780000000001",
  "title": "Database Systems Concepts",
  "publisherId": 1,
  "publisherName": "Higher Education Press",
  "originalPrice": 69.90,
  "salePrice": 59.90,
  "stock": 19,
  "status": "ON_SALE",
  "coverUrl": "https://example.com/cover.jpg"
}
```

### 3.2 图书详情

```http
GET /api/books/{bookId}
```

响应中除列表字段外，还包括：

```json
{
  "publishDate": "2024-09-01",
  "edition": "7th",
  "pages": 720,
  "description": "图书简介",
  "createTime": "2026-08-01T09:30:00",
  "updateTime": "2026-08-05T10:00:00",
  "authors": [
    { "id": 1, "name": "作者名" }
  ],
  "categories": [
    { "id": 2, "name": "Database" }
  ]
}
```

下架图书对用户端按不存在处理，返回 `404`。

### 3.3 分类列表

```http
GET /api/categories
```

只返回状态为 `1` 的分类，响应为数组：

```json
[
  {
    "id": 1,
    "name": "Computer Science",
    "parentId": null,
    "parentName": null,
    "sortOrder": 1,
    "status": 1
  }
]
```

### 3.4 作者列表

```http
GET /api/authors?keyword=database&page=1&size=10
```

响应为 `PageVo<AuthorVo>`。作者记录字段：

```json
{
  "id": 1,
  "name": "作者名",
  "country": "China",
  "introduction": "作者简介",
  "createTime": "2026-08-01T09:15:00",
  "updateTime": "2026-08-01T09:15:00"
}
```

### 3.5 出版社列表

```http
GET /api/publishers?keyword=press&page=1&size=10
```

响应为 `PageVo<PublisherVo>`。出版社记录字段：

```json
{
  "id": 1,
  "name": "出版社名称",
  "phone": "010-12345678",
  "address": "出版社地址",
  "introduction": "出版社简介",
  "createTime": "2026-08-01T09:10:00",
  "updateTime": "2026-08-01T09:10:00"
}
```

### 3.6 图书评价列表

```http
GET /api/books/{bookId}/reviews?page=1&size=10
```

无需登录。响应：

```json
{
  "bookId": 1,
  "averageRating": 4.8,
  "reviewCount": 12,
  "reviews": {
    "records": [
      {
        "id": 1,
        "bookId": 1,
        "bookTitle": "图书名称",
        "orderItemId": 10,
        "userId": 2,
        "reviewerName": "用户昵称",
        "rating": 5,
        "content": "评价内容",
        "status": 1,
        "createTime": "2026-08-04T16:00:00",
        "updateTime": "2026-08-04T16:00:00"
      }
    ],
    "total": 12,
    "page": 1,
    "size": 10,
    "totalPages": 2
  }
}
```

## 4. 用户资料与收货地址

以下接口都需要登录。

### 4.1 查询当前用户

```http
GET /api/user/me
```

### 4.2 修改当前用户资料

```http
PUT /api/user/me
```

请求体：

```json
{
  "nickname": "新昵称",
  "email": "new@example.com",
  "phone": "13800000000"
}
```

字段均可选。传空字符串会被转换为空值。

### 4.3 修改密码

```http
PUT /api/user/me/password
```

请求体：

```json
{
  "oldPassword": "old-password",
  "newPassword": "new-password",
  "confirmPassword": "new-password"
}
```

新密码长度为 6-50 个字符，原密码错误返回 `400`。

### 4.4 地址列表

```http
GET /api/user/addresses
```

默认地址会排在最前面。

### 4.5 地址详情

```http
GET /api/user/addresses/{addressId}
```

### 4.6 新增地址

```http
POST /api/user/addresses
```

请求体：

```json
{
  "receiverName": "张三",
  "receiverPhone": "13800000000",
  "province": "广东省",
  "city": "深圳市",
  "district": "南山区",
  "detailAddress": "科技园 1 号",
  "postalCode": "518000",
  "defaultAddress": true
}
```

说明：

- 第一个地址自动成为默认地址。
- `defaultAddress=true` 会取消该用户的原默认地址。
- `district`、`postalCode`、`defaultAddress` 可不传。

### 4.7 修改地址

```http
PUT /api/user/addresses/{addressId}
```

请求体字段与新增地址相同。该接口是完整更新接口，建议前端先读取详情再提交完整对象。

### 4.8 设置默认地址

```http
PUT /api/user/addresses/{addressId}/default
```

### 4.9 删除地址

```http
DELETE /api/user/addresses/{addressId}
```

删除默认地址后，系统会自动从剩余地址中选择一个新的默认地址。

## 5. 购物车接口

以下接口都需要登录。

### 5.1 查询购物车

```http
GET /api/cart
```

响应：

```json
{
  "items": [
    {
      "id": 1,
      "bookId": 3,
      "isbn": "9780000000003",
      "title": "Selected English Literature",
      "coverUrl": "https://example.com/cover.jpg",
      "salePrice": 79.90,
      "stock": 3,
      "bookStatus": "ON_SALE",
      "quantity": 1,
      "selected": true,
      "available": true,
      "subtotal": 79.90,
      "createTime": "2026-08-05T10:05:00",
      "updateTime": "2026-08-05T10:05:00"
    }
  ],
  "totalQuantity": 1,
  "selectedQuantity": 1,
  "selectedAmount": 79.90
}
```

`available=false` 表示图书已下架或当前库存不足，不能直接结算。

### 5.2 查询已选商品

```http
GET /api/cart/selected
```

响应结构同购物车查询。

### 5.3 加入购物车

```http
POST /api/cart/items
```

请求体：

```json
{
  "bookId": 3,
  "quantity": 1
}
```

数量范围为 `1-999`。如果购物车已有同一本书，会累加数量，并重新设置为选中。

### 5.4 修改购物车商品

```http
PUT /api/cart/items/{bookId}
```

请求体至少传一个字段：

```json
{
  "quantity": 2,
  "selected": true
}
```

`quantity` 和 `selected` 都是可选字段。数量仍需满足库存和 `1-999` 限制。

### 5.5 全选或取消全选

```http
PUT /api/cart/selection
```

请求体：

```json
{
  "selected": true
}
```

### 5.6 删除单个商品

```http
DELETE /api/cart/items/{bookId}
```

### 5.7 删除所有已选商品

```http
DELETE /api/cart/selected
```

响应 `data` 为实际删除的商品条数。

## 6. 订单与支付接口

### 6.1 查询我的订单

```http
GET /api/orders?status=PENDING_PAYMENT&page=1&size=10
```

`status` 可选，不传则查询全部订单。

订单响应示例：

```json
{
  "id": 2,
  "orderNo": "BS202608050001",
  "status": "PENDING_PAYMENT",
  "totalAmount": 79.90,
  "discountAmount": 0.00,
  "shippingFee": 0.00,
  "payableAmount": 79.90,
  "expireTime": "2026-08-05T10:40:00",
  "receiverName": "张三",
  "receiverPhone": "13800000000",
  "receiverAddress": "广东省 深圳市 南山区 科技园 1 号",
  "remark": "请尽快发货",
  "createTime": "2026-08-05T10:10:00",
  "updateTime": "2026-08-05T10:10:00",
  "paidTime": null,
  "shippedTime": null,
  "completedTime": null,
  "cancelledTime": null,
  "items": [
    {
      "id": 3,
      "bookId": 3,
      "bookTitle": "Selected English Literature",
      "isbn": "9780000000003",
      "unitPrice": 79.90,
      "quantity": 1,
      "subtotal": 79.90
    }
  ]
}
```

### 6.2 查询订单详情

```http
GET /api/orders/{orderId}
```

### 6.3 创建订单

```http
POST /api/orders
```

请求体：

```json
{
  "addressId": 1,
  "remark": "请尽快发货"
}
```

关键业务规则：

- 订单商品来源于当前用户购物车中 `selected=true` 的商品。
- 请求体不能指定商品，不能修改下单价格。
- 创建订单时立即扣减库存。
- 创建成功后，参与结算的购物车商品会被删除。
- 新订单状态为 `PENDING_PAYMENT`，默认支付有效期为 30 分钟。
- 库存不足或图书下架返回 `409`。
- 没有选中的购物车商品返回 `400`。

### 6.4 取消待支付订单

```http
PUT /api/orders/{orderId}/cancel
```

只有 `PENDING_PAYMENT` 状态可以取消。取消成功后会恢复库存，并写入
`ORDER_CANCEL_RETURN` 库存流水。

订单过期后，后台定时任务会自动取消订单并恢复库存，扫描间隔默认约为
60 秒。用户取消和后台超时取消使用条件更新保证并发安全，只有一个请求能成功改变订单状态。
因此，订单刚过期但定时任务尚未执行时，用户取消仍可能成功；如果订单已经被取消，
接口会返回 `409`。

### 6.5 模拟支付

```http
POST /api/orders/{orderId}/payment
```

请求体：

```json
{
  "paymentMethod": "MOCK"
}
```

当前实现不连接真实支付平台，调用成功后：

- 订单从 `PENDING_PAYMENT` 变为 `PENDING_SHIPMENT`。
- 返回一条 `PaymentVo`。
- 支付状态直接为 `SUCCESS`。
- 支付时会再次校验 `expireTime`；即使后台定时任务尚未执行，已过期订单也不能支付，返回 `409`。

支付响应：

```json
{
  "id": 1,
  "paymentNo": "PAY202608050001",
  "orderId": 2,
  "orderNo": "BS202608050001",
  "paymentMethod": "MOCK",
  "amount": 79.90,
  "status": "SUCCESS",
  "paidTime": "2026-08-05T10:20:00",
  "createTime": "2026-08-05T10:20:00"
}
```

### 6.6 确认收货

```http
PUT /api/orders/{orderId}/confirm-receipt
```

只有 `SHIPPED` 状态可以确认收货，成功后变为 `COMPLETED`。

## 7. 评价接口

### 7.1 查询我的评价

```http
GET /api/reviews/me
```

### 7.2 提交评价

```http
POST /api/reviews
```

请求体：

```json
{
  "orderItemId": 1,
  "rating": 5,
  "content": "内容清晰，例子很实用。"
}
```

限制：

- 订单必须为 `COMPLETED`。
- `orderItemId` 必须属于当前用户。
- 同一个订单明细只能评价一次。
- `rating` 范围为 `1-5`。
- `content` 最大长度为 1000。

### 7.3 修改评价

```http
PUT /api/reviews/{reviewId}
```

请求体：

```json
{
  "rating": 4,
  "content": "修改后的评价"
}
```

只能修改自己的评价。

## 8. 管理员接口

以下接口全部需要 `ADMIN` 角色。

### 8.1 图书封面上传

```http
POST /api/admin/uploads/images
Content-Type: multipart/form-data
```

表单字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `file` | file | 是 | JPG、PNG、GIF 或 WEBP 图片，最大 5MB |

示例：

```bash
curl -X POST "http://localhost:8080/api/admin/uploads/images" \
  -H "Authorization: Bearer <ADMIN_TOKEN>" \
  -F "file=@cover.jpg"
```

响应：

```json
{
  "url": "/uploads/covers/4f5b8f2e8d1a4d6eb1a0b9a7f2b9c123.jpg",
  "filename": "4f5b8f2e8d1a4d6eb1a0b9a7f2b9c123.jpg",
  "contentType": "image/jpeg",
  "size": 183420
}
```

说明：

- 当前实现使用本地 `uploads/covers` 目录存储文件。
- 返回的 `url` 可以直接作为新增或修改图书时的 `coverUrl`。
- 图片通过 `GET /uploads/covers/{filename}` 访问。
- 可通过环境变量 `APP_UPLOAD_DIR` 修改存储目录，
  `APP_UPLOAD_PUBLIC_URL_PREFIX` 修改 URL 前缀。
- 后续接入 MinIO、OSS 或 COS 时，可以保持接口不变，只替换文件存储服务。

### 8.2 图书管理

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `GET` | `/api/admin/books` | 分页查询全部图书，可按 `status` 筛选 |
| `GET` | `/api/admin/books/{bookId}` | 查询图书详情，包含下架图书 |
| `GET` | `/api/admin/books/low-stock?threshold=10` | 查询在售低库存图书 |
| `POST` | `/api/admin/books` | 新增图书 |
| `PUT` | `/api/admin/books/{bookId}` | 修改图书信息 |
| `PUT` | `/api/admin/books/{bookId}/status` | 上架或下架 |
| `DELETE` | `/api/admin/books/{bookId}` | 语义上等价于下架，不会物理删除 |
| `PUT` | `/api/admin/books/{bookId}/stock` | 调整库存并写库存流水 |

新增图书请求体：

```json
{
  "isbn": "9780000000001",
  "title": "Database Systems Concepts",
  "publisherId": 1,
  "originalPrice": 69.90,
  "salePrice": 59.90,
  "stock": 20,
  "publishDate": "2024-09-01",
  "edition": "7th",
  "pages": 720,
  "description": "图书简介",
  "coverUrl": "https://example.com/cover.jpg",
  "authorIds": [1],
  "categoryIds": [2]
}
```

新增规则：

- `isbn`、`title`、`publisherId`、价格、`authorIds` 必填。
- `authorIds` 至少包含一个作者。
- `categoryIds` 可以为空。
- `salePrice <= originalPrice`。
- `stock` 不传时默认为 `0`。
- 新增图书默认状态为 `ON_SALE`。

修改图书请求体为部分更新：

```json
{
  "title": "新书名",
  "salePrice": 49.90,
  "authorIds": [1, 2],
  "categoryIds": []
}
```

说明：

- `null` 表示不修改。
- `authorIds` 非 `null` 时整体替换作者，空数组不允许。
- `categoryIds` 非 `null` 时整体替换分类，空数组表示清空分类。
- 库存不要通过图书修改接口修改，应使用库存调整接口。

状态修改请求体：

```json
{
  "status": "OFF_SALE"
}
```

库存调整请求体：

```json
{
  "changeQuantity": 10,
  "remark": "补充库存"
}
```

`changeQuantity > 0` 表示入库，`changeQuantity < 0` 表示出库，不能为 `0`。

### 8.3 作者管理

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `GET` | `/api/admin/authors?keyword=&page=1&size=10` | 分页查询 |
| `GET` | `/api/admin/authors/{authorId}` | 查询详情 |
| `POST` | `/api/admin/authors` | 新增作者 |
| `PUT` | `/api/admin/authors/{authorId}` | 修改作者 |
| `DELETE` | `/api/admin/authors/{authorId}` | 删除作者 |

新增或修改请求体：

```json
{
  "name": "作者名",
  "country": "China",
  "introduction": "作者简介"
}
```

被图书关联的作者不能删除。

### 8.4 出版社管理

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `GET` | `/api/admin/publishers?keyword=&page=1&size=10` | 分页查询 |
| `GET` | `/api/admin/publishers/{publisherId}` | 查询详情 |
| `POST` | `/api/admin/publishers` | 新增出版社 |
| `PUT` | `/api/admin/publishers/{publisherId}` | 修改出版社 |
| `DELETE` | `/api/admin/publishers/{publisherId}` | 删除出版社 |

请求体：

```json
{
  "name": "出版社名称",
  "phone": "010-12345678",
  "address": "出版社地址",
  "introduction": "出版社简介"
}
```

被图书关联的出版社不能删除。

### 8.5 分类管理

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `GET` | `/api/admin/categories?status=1` | 查询分类，可按状态筛选 |
| `GET` | `/api/admin/categories/{categoryId}` | 查询详情 |
| `POST` | `/api/admin/categories` | 新增分类 |
| `PUT` | `/api/admin/categories/{categoryId}` | 修改分类 |
| `PUT` | `/api/admin/categories/{categoryId}/status` | 修改启用状态 |
| `DELETE` | `/api/admin/categories/{categoryId}` | 删除分类 |

新增或修改请求体：

```json
{
  "name": "数据库",
  "parentId": 1,
  "sortOrder": 1,
  "status": 1
}
```

分类不能把自己或自己的后代设置为父分类；有子分类或被图书关联的分类不能删除。

状态修改请求体：

```json
{
  "status": 0
}
```

### 8.6 订单管理

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `GET` | `/api/admin/orders` | 分页查询订单 |
| `GET` | `/api/admin/orders/{orderId}` | 查询订单详情 |
| `PUT` | `/api/admin/orders/{orderId}/ship` | 待发货订单发货 |

查询参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `orderNo` | string | 订单号模糊查询 |
| `userId` | number | 用户 ID |
| `status` | enum | 订单状态 |
| `page` | number | 默认 `1` |
| `size` | number | 默认 `10` |

只有 `PENDING_SHIPMENT` 订单可以发货，发货后变为 `SHIPPED`。

### 8.7 后台统计

```http
GET /api/admin/statistics/overview?months=6&top=10&lowStockThreshold=5
```

参数范围：

- `months`：`1-24`，默认 `6`
- `top`：`1-100`，默认 `10`
- `lowStockThreshold`：`0-100000`，默认 `5`

响应：

```json
{
  "completedOrderCount": 3,
  "salesAmount": 99.90,
  "soldQuantity": 7,
  "monthlySales": [
    {
      "saleMonth": "2026-08",
      "completedOrderCount": 3,
      "soldQuantity": 7,
      "salesAmount": 99.90
    }
  ],
  "topBooks": [
    {
      "bookId": 1,
      "bookTitle": "图书名称",
      "soldQuantity": 4,
      "salesAmount": 60.00
    }
  ],
  "categorySales": [
    {
      "categoryId": 2,
      "categoryName": "数据库",
      "soldQuantity": 4,
      "salesAmount": 60.00
    }
  ],
  "lowStockBooks": [
    {
      "bookId": 2,
      "isbn": "9780000000002",
      "title": "库存较低的图书",
      "stock": 2,
      "status": "ON_SALE"
    }
  ]
}
```

### 8.8 评价审核

#### 分页查询评价

```http
GET /api/admin/reviews
```

查询参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `bookId` | number | 否 | 按图书筛选 |
| `userId` | number | 否 | 按用户筛选 |
| `status` | number | 否 | `1` 正常、`0` 屏蔽 |
| `page` | number | 否 | 默认 `1` |
| `size` | number | 否 | 默认 `10`，最大 `100` |

不传 `status` 时返回正常和屏蔽的全部评价；公众图书评价接口只返回
`status=1` 的评价。

#### 查询评价详情

```http
GET /api/admin/reviews/{reviewId}
```

#### 审核、屏蔽或恢复评价

```http
PUT /api/admin/reviews/{reviewId}/status
```

请求体：

```json
{
  "status": 0
}
```

`status=0` 会在公众评价列表中隐藏该评价，`status=1` 会恢复展示。

### 8.9 库存流水查询

```http
GET /api/admin/inventory-logs
```

查询参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `bookId` | number | 否 | 按图书筛选 |
| `orderId` | number | 否 | 按关联订单筛选 |
| `changeType` | enum | 否 | `PURCHASE_IN`、`ORDER_OUT`、`ORDER_CANCEL_RETURN`、`MANUAL_ADJUSTMENT` |
| `startTime` | datetime | 否 | 起始时间，ISO-8601 |
| `endTime` | datetime | 否 | 结束时间，ISO-8601，包含该时刻 |
| `page` | number | 否 | 默认 `1` |
| `size` | number | 否 | 默认 `10`，最大 `100` |

时间参数示例：

```text
/api/admin/inventory-logs?bookId=1&startTime=2026-08-01T00:00:00&endTime=2026-08-06T23:59:59
```

响应 `data` 为 `PageVo<InventoryLogVo>`：

```json
{
  "records": [
    {
      "id": 6,
      "bookId": 3,
      "isbn": "9780000000003",
      "bookTitle": "Selected English Literature",
      "changeQuantity": -2,
      "beforeStock": 5,
      "afterStock": 3,
      "changeType": "ORDER_OUT",
      "orderId": 2,
      "orderNo": "BS202608050001",
      "remark": "订单创建扣减库存",
      "createTime": "2026-08-05T10:10:00"
    }
  ],
  "total": 1,
  "page": 1,
  "size": 10,
  "totalPages": 1
}
```

库存流水按创建时间倒序返回，可用于后台库存审计、补货核对和订单取消回库追踪。

统计口径以已完成订单 `COMPLETED` 为准。

## 9. 前端推荐实现顺序

1. 登录/注册和 Token 持久化。
2. 图书列表、详情、分类筛选、评价展示。
3. 用户资料和收货地址。
4. 购物车增删改查和全选。
5. 创建订单、订单列表、模拟支付、取消订单。
6. 确认收货和评价。
7. 管理员图书、库存、订单、分类、作者、出版社。
8. 后台统计图表。

## 10. 很值得做的优化

### P0：已完成的基础修复

1. **订单取消和支付状态条件已修复**

   取消订单不再使用错误的 `:paidTime` 参数，超时扫描可以正常取消过期订单；
   支付更新使用 `:paidTime` 校验支付时是否已经过期。订单状态更新仍然使用条件更新，
   可以避免重复取消、重复支付和支付/超时取消并发导致重复回库。

2. **UTF-8 编码已统一**

   项目已补充 Maven UTF-8 编译配置、MySQL `utf8mb4` 连接配置和 `.editorconfig`。
   如果终端显示乱码，请优先检查终端代码页，不要直接修改源码编码。

3. **数据库初始化策略已统一**

   当前配置为 `spring.jpa.hibernate.ddl-auto=validate`。请先执行数据库脚本，
   再启动后端；后续修改表结构时应同步更新 SQL 脚本和实体映射。

4. **价格和数量的 Service 层校验已补充**

   图书创建/更新、库存调整和订单创建流程已补充 DTO 非空、价格非负、
   售价不高于原价、库存非负、页数大于 0、作者/分类 ID 有效以及商品数量
   `1-999` 等校验。Controller 校验和 Service 校验会同时生效。

### P1：前端正式联调前建议完成

1. **增加 OpenAPI/Swagger 文档**

   用 `springdoc-openapi` 直接生成 `/v3/api-docs`，前端可以导入接口定义，减少手写字段和枚举错误。

2. **统一错误码，而不是只使用 HTTP 状态码**

   建议把“库存不足、图书下架、订单状态不允许、用户名重复”等业务错误拆成稳定的错误码，前端根据错误码展示提示，而不是依赖中文 message。

3. **订单支付增加幂等机制**

   当前重复点击支付会依赖订单状态竞争返回 `409`。建议支持 `Idempotency-Key`，并为订单增加支付状态唯一约束或支付请求号，避免网络重试造成重复支付记录。

4. **订单创建增加幂等机制**

   用户在网络超时后重试创建订单，可能产生多个订单。建议前端传递请求幂等号，后端保存并返回原订单。

5. **补充支付查询、退款和支付回调接口**

   当前支付接口是模拟支付，真实支付至少还需要支付结果查询、异步回调、签名校验、支付关闭和退款流程。

6. **图片上传接口已增加**

   已增加管理员图片上传接口 `POST /api/admin/uploads/images`，当前使用本地目录保存并返回可访问的 `coverUrl`。
   后续可以在不改变接口的情况下，将文件存储服务替换为 MinIO、阿里云 OSS 或腾讯云 COS。

7. **后台评价审核接口已增加**

   已增加管理员评价分页查询、详情查询以及 `status=0/1` 屏蔽和恢复接口。
   公众评价列表只展示 `status=1` 的评价。

8. **库存流水查询接口已增加**

   已增加 `/api/admin/inventory-logs` 分页接口，支持按图书、订单、变更类型和时间范围筛选，
   并补充了创建时间索引，便于后台审计和库存追踪。

### P2：项目质量和性能优化

1. **用 Spring Security Filter 替换手写 JWT Interceptor**

   当前 Spring Security 实际上对所有请求 `permitAll`，权限主要依赖 MVC Interceptor。建议把 JWT 解析、用户认证和角色授权统一放到 Security FilterChain 中，减少认证链路分散的问题。

2. **生产环境禁止使用默认 JWT 密钥**

   `application.properties` 中存在默认 JWT secret。应当要求通过环境变量注入强随机密钥，并避免把示例密码和默认数据库密码用于部署环境。

3. **登录增加限流和失败次数控制**

   登录接口目前没有验证码、限流或账户锁定机制，容易遭受密码暴力尝试。课程项目至少可以增加基于 IP/用户名的简单限流。

4. **优化图书列表查询**

   当前搜索只能按书名，且多个筛选条件不是组合过滤。建议改成 Specification 或 QueryDSL，支持关键词、分类、作者、出版社、价格区间、库存状态和排序组合查询。

5. **避免统计接口一次返回过多分类数据**

   分类销售统计目前没有 `top` 或分页限制，数据量大时响应会变慢。建议增加分类统计 Top N 或分页参数。

6. **关注懒加载和 N+1 查询**

   图书详情、订单列表会额外查询作者、分类或订单明细。数据规模扩大后建议使用 DTO 投影、批量查询或 `EntityGraph`。

7. **增加 Controller 层集成测试**

   当前测试主要覆盖 Service 和 Mockito 场景。建议补充 MockMvc 测试，覆盖 JWT、管理员权限、参数校验、HTTP 状态码和统一响应格式。

8. **增加订单状态流转图和数据库约束测试**

   推荐固定允许的状态迁移：

   ```text
   PENDING_PAYMENT -> PENDING_SHIPMENT -> SHIPPED -> COMPLETED
   PENDING_PAYMENT -> CANCELLED
   ```

   并针对重复支付、重复取消、超时取消与支付并发进行集成测试。

## 11. 建议前端封装

前端可以统一封装以下能力：

- `request`：自动添加 Token、解析 `{code,message,data}`。
- `401`：清除 Token 并跳转登录页。
- `403`：提示无管理员权限。
- `409`：显示库存、订单状态或重复数据提示。
- `PageVo<T>`：统一封装分页列表。
- `OrderStatus` 和 `BookStatus`：使用枚举映射中文展示文本。
- 金额：使用字符串或十进制库处理，展示时保留两位小数。
- 时间：后端返回 ISO-8601 格式，例如 `2026-08-05T10:20:00`。
